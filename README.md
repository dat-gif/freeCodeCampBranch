# freeCodeCamp Bracnh API

## Installation

Use the package manager yarn. On root folder run:

```bash
yarn install
```

## Usage

On root folder:

```shell
# run both client and server
yarn start

# only server
yarn workspace server start

# only client
yarn workspace client start
```
